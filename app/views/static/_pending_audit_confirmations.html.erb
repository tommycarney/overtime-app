<% pending_audit_confirmations.each do |pending_audit_confirmation| %>
  <%= link_to "I can confirm that I did not perform any overtime for the week of: #{pending_audit_confirmation.start_date}", audit_log_path(pending_audit_confirmation), class: "btn btn-primary btn-block btn-lg", data: { confirm: "Are you sure you want to confirm that you did not do any overtime?"} %>

<% end %>
